<%= form_with(model: log, local: true) do |form| %>
  <% if log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(log.errors.count, "error") %> prohibited this log from being saved:</h2>

      <ul>
      <% log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :action, class: 'active' %>
    <%= form.text_field :action %>
  </div>

  <div class="field">
    <%= form.label :date, class: 'active' %>
    <%= form.datetime_select :date %>
  </div>

  <div class="field">
    <%= form.label :table, class: 'active' %>
    <%= form.text_field :table %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
